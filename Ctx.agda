-------------------------------------------------------------------------------
-- Contexts (Snoc Lists)
-------------------------------------------------------------------------------

module Ctx where

  infixl 1 _▸_
  data Ctx (α : Set) : Set where
    ε   : Ctx α
    _▸_ : Ctx α → α → Ctx α
